# 越位识别系统 — 分步项目计划

**创建日期**: 2026-06-16  
**团队**: Anne(项目经理/主开发) · Ada(测试工程师) · Alex(技术探索)

---

## 总览

| 步骤 | 名称 | 目标 | 依赖 | 状态 |
|------|------|------|------|------|
| Step 1 | 球场标定验证 | 标定点位置正确，投影对齐 | 无 | 🔴 待开始 |
| Step 2 | Homography精度 | 单应矩阵准确，越位线平行 | Step 1 | 🔴 待开始 |
| Step 3 | 球员检测验证 | 球员检出+脚部定位准确 | Step 2 | 🔴 待开始 |
| Step 4 | 越位判定逻辑 | IFAB规则正确，越位线位置对 | Step 2+3 | 🔴 待开始 |
| Step 5 | 可视化验证 | 画面标注清晰正确 | Step 1-4 | 🔴 待开始 |
| Step 6 | 端到端集成 | 完整视频输出正确 | Step 1-5 | 🔴 待开始 |

---

## Step 1: 球场标定验证

### 目标
32关键点检测 + 世界坐标匹配完全正确。球场模板线投影到图像上必须对齐。

### 背景：当前问题
- 越位线倾斜（不平行于球门线）— 说明homography有问题
- 标定是后续所有步骤的基础，标定错了后面全错

### 当前代码位置
`offside_detector/pitch_keypoint_detector.py`
- `_build_world_coords()`: 32个关键点世界坐标（Roboflow 120×70）
- `compute_homography()`: RANSAC找单应矩阵
- `calibrate()`: 完整标定流程

### 计划修改（Anne）

#### 1.1 添加标定可视化函数
```python
def draw_pitch_overlay(frame, H, alpha=0.5):
    """将FIFA球场关键线段通过H反投影到帧上，用于可视验证"""
    # 画：禁区线、小禁区线、中圈、中线、球门线
```

#### 1.2 添加标定质量指标
```python
def calibrate_quality_metrics(src_pts, dst_pts, H):
    """返回：重投影RMSE(像素), 内点比例, 各关键点误差"""
```

#### 1.3 确认关键点顺序正确
- 对照官方Roboflow SoccerPitchConfiguration的32个label定义
- 确保每个索引的世界坐标一一对应正确

### Ada测试方法
1. 写 `tests/test_step1_calibration.py`，从视频抽10帧
2. 每帧运行关键点检测 + 标定
3. 输出每帧的标定叠加图（球场线覆盖原图）
4. 人眼检查10张叠加图的对齐程度
5. 记录重投影RMSE
6. **只允许重投影RMSE < 8px的标定通过**

### 通过标准
- ✅ 所有10帧的标定叠加图中，球场线（禁区边线、中圈、球门线）与图像中实际线条基本重合
- ✅ 重投影RMSE < 8px
- ✅ 内点比例 > 50%（32个点中至少16个inlier）

---

## Step 2: Homography精度优化

### 目标
单应矩阵计算鲁棒化，确保越位线（world_y等高水平线）投影到图像上平行于真实球门线。

### 背景：当前问题
- RANSAC阈值10米（应该是像素！）— 这是主要bug
- 无几何约束，远端点误差导致越位线倾斜
- 无迭代优化，一次RANSAC定生死

### 参考开源方案
**SoccerNet #1方案** 的核心做法：
1. RANSAC阈值 = **5px**（不是米）
2. **多子集投票**：在不同关键点子集上分别计算homography，选RMSE最低的
3. **迭代Voter**：逐步纳入不同置信度阈值的关键点
4. **相机参数合理性检查**：camera不能在地下/天上，焦距不能异常

### 计划修改（Anne）

#### 2.1 修复RANSAC阈值单位
```python
# 当前(错误):
ransacReprojThreshold=10.0  # 10米 - 太松

# 修正:
ransacReprojThreshold=5.0   # 5像素 - 业界标准
```

#### 2.2 添加多子集Voter
```python
def voter_homography(src_pts, dst_pts, confidences):
    """多子集投票选最优homography:
    子集1: 所有点 (置信度>0.3)
    子集2: 高置信度点 (>0.5)
    子集3: RANSAC过滤后inliers
    → 选重投影RMSE最低的
    """
```

#### 2.3 添加几何约束验证
```python
def validate_homography(H):
    """检查：
    1. 球门线平行度：world_to_pixel(0,0)→world_to_pixel(68,0) 角度 vs world_to_pixel(0,105)→world_to_pixel(68,105)
    2. 球场不扭曲：四个角点投影后依然是凸四边形
    3. 相机方向合理性
    """
```

### Ada测试方法
1. 写 `tests/test_step2_homography.py`
2. 对10帧计算homography
3. **关键测试**：在world_y=0和world_y=105（两条球门线）处投影到图像，检查两条线段的夹角 — 应接近0度
4. 检查越位侧场地：world_to_pixel(0,52.5) 应大致在图像中场位置
5. 记录每条homography的RMSE、内点比例、门线平行度

### 通过标准
- ✅ 球门线平行度 < 2度（world_y=0和world_y=105投影线的夹角）
- ✅ 重投影RMSE < 5px
- ✅ 所有测试帧通过相机合理性检查
- ❌ 不做：直接看越位线是否平行于球门线（留给Step 5做）

---

## Step 3: 球员检测验证

### 目标
球员检出率、脚部定位、队服分类准确。

### 当前代码位置
`offside_detector/player_detector.py`
- YOLOv8n检测 + ByteTrack跟踪 + HSV K-means分队

### 计划修改（Anne）

#### 3.1 增加球员foot_position精度评估
- 当前用bounding box底部中心近似脚位置
- 可选：使用YOLOv8-pose做脚踝检测（如果精度不够）

#### 3.2 队服分类验证
- 输出每个球员的HSV颜色和分配到的队伍

### Ada测试方法
1. 对10帧运行球员检测
2. 输出每帧的球员框+脚点+队伍标注图
3. 数检出球员数，判断是否漏检
4. 检查队服分类是否正确（眼睛验证）

### 通过标准
- ✅ 每帧检出球员数 ≥ 实际场上球员的80%
- ✅ 队服分类100%正确（手动确认）
- ✅ 脚部位置无明显偏移

---

## Step 4: 越位判定逻辑

### 目标
IFAB Law 11越位规则正确实现。

### 当前代码位置
`offside_detector/offside_analyzer.py`

### 关键逻辑（需验证）
1. 倒数第二名防守球员识别（门将通常为第一名）
2. 攻击方球员的world_y与越位线比较
3. 攻击方向检测（应向球门方向）
4. 边界情况：球员齐平不算越位（0.1m容差）

### 计划修改（Anne）
- 如果有bug则修复，逻辑本身应该是对的
- 主要工作是增加可测试性：输出每帧详细的判定中间结果

### Ada测试方法
1. **测试已知帧**：找能明确看出谁越位/谁不越位的帧
2. 输出每帧的判定明细：
   - 攻击方向
   - 倒数第二名防守球员（标注是谁）
   - 越位线world_y
   - 每个攻击方球员的world_y和判定
3. 4-6个已知案例验证

### 通过标准
- ✅ 所有已知案例判定正确
- ✅ 越位线位置 = 倒数第二名防守球员脚部world_y
- ✅ 边界情况（齐平）正确处理

---

## Step 5: 可视化验证

### 目标
画面标注清晰准确，越位线平行于球门线。

### 当前代码位置
`offside_detector/visualizer.py`

### 关键检查
- 越位线是否从左边线画到右边线
- 越位区域半透明覆盖是否正确
- 球员框颜色（红色=越位，绿色=不越位，蓝色=倒数第二名防守者）
- 小地图位置标注是否正确

### Ada测试方法
1. 对已通过Step 1-4的帧运行完整可视化
2. 截图检查：
   - 越位线是否平行于球门线 ✅
   - 越位球员是否被红色框圈出
   - 小地图上位置是否与主画面一致

### 通过标准
- ✅ 越位线平行于球门线
- ✅ 球员框颜色正确
- ✅ 小地图位置一致

---

## Step 6: 端到端集成

### 目标
完整视频处理，输出可用的教学素材。

### Ada测试方法
1. `python run.py --video "13-有意处理球.mp4" --calib-mode auto --resize 960`
2. 检查输出：
   - 视频可播放，帧率正常（不加速）
   - 越位帧有正确标注
   - JSON分析数据完整

### 通过标准
- ✅ 输出视频可正常播放
- ✅ 帧率正确（= 原帧率 / process_every_n_frames）
- ✅ 至少1帧检出越位
- ✅ JSON数据完整

---

## Alex的研究任务清单

| 任务 | 状态 | 关键发现 |
|------|------|----------|
| 球场关键点检测最佳实践 | ✅ 完成 | SoccerNet: 57点, RANSAC 5px阈值, 迭代Voter |
| 现有越位检测项目分析 | ✅ 完成 | hamzaokd: 概念验证, 全手动标定 |
| TVCalib论文分析 | 🔴 待完成 | 端到端可微分标定 |
| 更多开源项目 | 🔴 待完成 | 搜 "football offside VAR python" |
