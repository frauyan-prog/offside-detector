# 越位识别项目 — 进度备忘

**日期**: 2026-08-31
**当前阶段**: 排查「镜头摇动导致越位线偏移」

> 本文为跨会话续接用。下次开工先读本文件，再读 `PROJECT_INTRO.md` + `AUDIT_INVENTORY.md` 补齐背景。

---

## 一、项目方向（用户 2026-08-31 确认）

用户提出过两个方向，**最终选定方向二**：

| 方向 | 内容 | 状态 |
|------|------|------|
| 方向一 | 用一张图片恢复成 3D 场景（相机位姿估计/三维重建） | ❌ 已放弃，代码中无痕迹 |
| **方向二** | 模拟转播画面，画出越位线，并标注两队球员 | ✅ **当前实现** |

方向二的本质：把球场当作**平面**，用单应性矩阵 H 把像素映射到 FIFA 世界坐标（68×105m），再反投影画越位线。核心模块 `view_transformer.py` + `offside_analyzer.py` + `visualizer.py`。

---

## 二、要解决的核心问题

**用户原话**：拍摄机位是"类似转头"的方式（摇摄/变焦），整个球场的比例定位会改变，所以坐标不对，越位线有点偏。

**症状（用户已确认）**：**镜头一动就偏，动完不恢复**。（不是持续偏、不是抖动、不是静止时准）

**标定模式（用户已确认）**：`center_circle` —— 手动点中圈直径两点。

⚠️ **auto 模式不可用（用户 2026-08-31 明确确认）**：用户此前长期测试 auto 模式，**始终不能用**，因此才改用两点标定。
**明天不要再试 auto 模式**，直接围绕 `center_circle` 想办法（原计划的 Step 1 已作废，见第七节）。

### 根因方向

homography 的前提假设是**相机不动**。镜头一摇，画面里球场的比例位置全变，旧 H 立刻失效。代码的应对机制是**周期性重标定**：

```317:321:offside_detector/video_processor.py
            # --- Step 0: Auto-recalibrate Homography periodically (handles camera movement) ---
            if (config.field_detect_every_n_frames > 0
                    and processed_count > 0
                    and processed_count % config.field_detect_every_n_frames == 0):
                self._recalibrate_from_current_frame(frame, frame_idx)
```

每 10 帧重算一次 H。**本次调查的核心结论就是：这套机制在当前镜头下完全没起作用。**

---

## 三、今天做了什么

1. 从 git log / 工作区文档恢复上下文（无历史对话记忆）
2. 梳理 4 个未提交文件的改动
3. 改造 `test_run.py`，支持 `center_circle` 模式 + 自动加载标定文件
4. 用 `center_circle` 模式跑 150 帧诊断测试，抓重标定日志
5. 结合 `output/` 下遗留的诊断图分析失败原因

---

## 四、关键发现

### 4.1 🔴 两条重标定路径全部失效，H 从未更新

诊断日志（`python test_run.py 13 --calib-mode center_circle --max-frames 150`）：

```
[VideoProcessor] Frames: 736, FPS: 30.0, Size: 1184x660
[CenterCircle] 2-point calibration: line=110px → 18.3m, synthesised 4 points for homography
[EllipseCalib] No ellipse detected
  [Recalib Frame 10] Ellipse not detected
[EllipseCalib] No ellipse detected
  [Recalib Frame 20] Ellipse not detected
...（14 次重标定，14 次全部失败，直到 Frame 140）
```

**统计**：
- 椭圆路径：14 次触发，**14 次失败**
- VP 路径：静默 `return`，**一次成功日志都没有**（成功会打印 `[Recalib VP Frame X] VP=(...)`）
- **净效果：H 从第 0 帧到第 150 帧从未更新过**

这就是"动完不恢复"的直接原因。

### 4.2 椭圆路径为什么失败：几何方法不适合

`detect_center_circle_ellipse` 的流程是：灰度阈值 → 与绿色掩膜求交 → Canny → `findContours` → `fitEllipse`。

看 `output/calib_white_lines_mask.png`：中圈白线**能提取出来，但是被球员和草皮条纹打断的断续弧线**，不是完整闭合轮廓。断续弧线会裂成多个小轮廓，每个都不够大，无法拟合出完整中圈。

再加上中圈位于画面**左侧**（x≈175，画面中心 x=592），而打分函数惩罚偏离画面中心的候选：

```119:121:offside_detector/ellipse_calibrator.py
            # Score: prefer ellipses near image center, with good aspect ratio
            center_dist = np.sqrt((cx - w/2)**2 + (cy - h/2)**2) / max(w, h)
            score = ar * (1.0 - center_dist * 0.5)
```

镜头 pan 之后中圈本来就该偏离中心，这个惩罚项是反效果。

### 4.3 VP 路径为什么失败：镜头无消失点

看 `output/calib_lines_detect.png`：球场线检测**是成功的**（大量绿线），但几乎都平行 —— 这是**远距离弱透视镜头**，平行线消失点在无穷远处。

```655:657:offside_detector/video_processor.py
            vp, vp_quality = PitchKeypointDetector.find_vp_from_lines(field_lines)
            if vp is None or vp_quality < 0.3:
                return          # ← 静默失败，不打印任何日志
```

VP 质量 < 0.3 → `return`。**注意这三个 early return 都是静默的**，给排查造成很大干扰，建议后续加日志。

### 4.4 🟡 潜在 bug：VP 路径用第一帧的旧点击点（暂未暴露）

**证据** —— `output/13-有意处理球_center_circle_calib.json`：

```json
{
  "calibration_mode": "center_circle",
  "video_width": 1184,
  "video_height": 660,
  "pixel_points": [[170.0, 250.0], [180.0, 360.0]],
  "world_points": [[34.0, 61.65], [34.0, 43.35]]
}
```

这两个点是**第一帧**手动点的中圈位置，被读进 `_field_result.keypoints` 后，在**每一次** VP 重标定里被当作"当前帧中圈位置"：

```659:671:offside_detector/video_processor.py
            stored_kpts = self._field_result.keypoints
            attack_dir = self._field_result.attack_dir

            if stored_kpts is not None and len(stored_kpts) >= 2:
                # Path A: Use stored click points + new VP
                from .field_detector import CenterCircleCalibrator
                cc = CenterCircleCalibrator()
                pixel_pts = [(float(stored_kpts[i][0]), float(stored_kpts[i][1]))
                             for i in range(min(2, len(stored_kpts)))]
                vp_result = cc._build_from_vp(
                    frame, pixel_pts, attack_dir=attack_dir,
                    vp=vp, vp_quality=vp_quality, kp_detector=None
                )
```

矛盾在于：**VP 是从当前帧算的（正确），两个点是第一帧的（错误）**，混合解 DLT → H 整体平移偏移。

而且重标定成功后也**不更新 keypoints**：

```539:540:offside_detector/video_processor.py
                self._field_result.homography = H
                self._field_result.inverse_homography = np.linalg.inv(H)
```

**为什么 3° 质量门控拦不住**：用错点算出的 H，球门线依然可以平行（角度 < 3°），只是整体**平移**了。门控只查角度，查不出平移 —— 与用户"越位线偏（位置偏，非倾斜）"的症状吻合。

⚠️ **但此 bug 目前没机会暴露**，因为 VP 路径压根没成功过一次。修好 VP 路径后它才会显形，届时必须一起修。

### 4.5 auto 模式历史产物质量崩坏

`output/13-有意处理球_offside_calibration.json`（auto 模式产物）：

```json
"quality": {
    "mean_confidence": 0.5081,
    "n_detected": 32,
    "n_used": 20,
    "n_inliers": 0,
    "calibration_rmse_m": 29296.475,
    "calibration_rmse_px": 802.3,
    "voter_rmse_m": 34.127,
    "subset": "line_vp"
}
```

0 内点、重投影误差 802px —— auto 模式在这个视频上基本失败，这解释了用户为何转向 `center_circle`。
⚠️ 但这份文件可能是**加质量门控之前的旧产物**，当前代码可能已改善。

### 4.6 其他观察：越位判定疑似异常

```
[VideoProcessor] Frame 149: 150 processed, 77 offside, 14.0 fps
[Defenders] (2 total) / (1 total) / (3 total)
```

- 150 帧里 **77 帧判越位**（一半以上）→ 不合常理，越位不该是常态
- 每帧防守方只识别到 **1-3 人** → 疑似球员漏检严重

⚠️ 未深入排查，但这是独立于标定之外的隐患，修完标定后很可能需要处理。

### 4.7 ✅ 已修：center_circle 的点击提示写错了（用户提出）

**用户反馈**：提示只说 left / right，无法判断是按进攻方还是防守方的视角，建议改成「远点 / 近点」。

**核查结论：质疑成立，提示确实是错的** —— 而且不只是措辞问题。

- 提示原文（已改）：「点击中圈**水平直径**的两个端点 / 左端点 / 右端点」
- 代码实际逻辑：按**画面上下位置**判断远近，与左右、与攻守双方**都无关**

```966:973:offside_detector/field_detector.py
            if p1_py > p2_py:
                # p1 is lower in image (near goal) -> world y = cy - r
                world_pts = np.array([
                    [cx,      cy - r],    # 0: p1 -> near goal side
                    [cx,      cy + r],    # 1: p2 -> far goal side
```

用标定文件反推验证过：用户点的 (170,250) / (180,360) 是一上一下，系统正确映射为
(34,61.65) / (34,43.35) —— 与 `13-有意处理球_center_circle_calib.json` 的 `world_points` **完全吻合**。

**两个附加结论**：

1. **点击顺序无所谓** —— `if p1_py > p2_py` 会自动纠正，点反了结果相同
2. **沿纵深点（上下 / 远近）比点左右更合理** —— 越位沿纵深方向判定，纵深直径能直接约束纵深尺度；点成左右的话，纵深尺度要靠合成的虚拟点推算，精度更差

**已做的修改**（`field_detector.py` 的 `CenterCircleCalibrator`）：

| 项 | 改动 |
|------|------|
| `INSTRUCTIONS` | 中文「近点 / 远点」说明，并解释为何不点左右 |
| `CLICK_LABELS` | 英文 `NEAR point (lower on screen)` / `FAR point (upper on screen)` |
| `TITLE` | 改为英文 |
| 类 docstring | 补全正确的世界坐标映射（原 docstring 只描述了"水平直径"一种情况，易误导） |

⚠️ **附带发现：OpenCV 窗口上的中文全是乱码**

`cv2.putText` 使用 Hershey 字体，**不支持 CJK**，中文会被画成 `???`。
因此画面文本必须保持 ASCII，中文说明只能放在终端打印的 `INSTRUCTIONS` 里。

**其他标定模式（PenaltyArea / FourCorner / Generic）的 `TITLE` 和 `CLICK_LABELS` 仍是中文，存在同样的乱码问题，本次未修。**

验证：`python -m py_compile offside_detector/field_detector.py` → SYNTAX OK，lint 无错误。

---

## 五、关于 CLAHE（未完成的改动）的结论

`ellipse_calibrator.py` 里有处改了一半的代码：

```74:78:offside_detector/ellipse_calibrator.py
        gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
        # Use CLAHE for contrast enhancement (helps with uneven broadcast lighting)
        clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8, 8))
        gray_eq = clahe.apply(gray)
        _, white_mask = cv2.threshold(gray, 180, 255, cv2.THRESH_BINARY)
```

`gray_eq` 算了却没用（threshold 仍用 `gray`）。

**当初的意图**：提升白线提取对光照不均的鲁棒性 → 提高椭圆检测成功率 → 让重标定走椭圆路径（唯一不依赖旧点击点的路径）。

**当前判断**：**接上大概率无效，建议暂缓**。
因为椭圆检测失败的根因是**几何方法**（断续弧线无法 `fitEllipse`），不是亮度问题。白线在掩膜图里已经能被提取出来了。

⚠️ 中途修正过一次判断：最初基于"用户用 auto 模式"误判 CLAHE 不在主链路上、应搁置；用户澄清用 `center_circle` 后已纠正 —— CLAHE **确实**在链路上，只是解决不了根因。

---

## 六、当前代码状态

### 未提交改动（4 文件 +118 行）

| 文件 | 改动 | 状态 |
|------|------|------|
| `video_processor.py` | +109 行：三条重标定路径的统一质量门控（球门线夹角 ≤3° + 四角凸性 + 帧间连续性保护） | ✅ 完整 |
| `offside_analyzer.py` | 调试日志从逐帧改为每 30 帧节流（打印 IO 曾是性能瓶颈）；移除逐帧 DEBUG 打印 | ✅ 完整 |
| `pitch_keypoint_detector.py` | 兼容 cv2 4.x/5.x `HoughLines` 返回形状差异 `(N,1,4)` vs `(N,4)` | ✅ 完整 |
| `ellipse_calibrator.py` | CLAHE（**半成品**，`gray_eq` 未接上） | ❌ 未完成 |

已验证：`python -m py_compile` 对 4 个文件全部 `SYNTAX OK`。

### 最近提交（都在攻重标定）

```
7cac480 fix: wider line detection angles, VP recalibration diagnostic logging, fix _recalibrate_auto regression
5780da5 feat: VP recalibration with synthetic points for ellipse modes (no stored click points)
27eea06 feat: add recalibration debug logging to diagnose ellipse/VP failures
b8c7ce4 fix: correct horizontal field LEFT_TO_RIGHT defender direction (defending goal at x=68)
af1ae28 fix: horizontal field defender sort direction (left_to_right = ASC world_x / pixel_x)
```

### 未跟踪文件

- `test_run.py` —— 本次已改造（见下）

---

## 七、明天接着做（按优先级）

### ~~Step 1：验证 auto 模式~~ ❌ 已作废

用户 2026-08-31 明确确认 **auto 模式一直不能用**，不必再试。

### Step 1（新）：给 center_circle 换一条能用的重标定路径 ⭐ 最优先

前提：椭圆路径与 VP 路径对当前镜头双双失效（4.1–4.3），auto 又不可用 → H 从未更新过。必须让 `center_circle` 自己能重标定。

候选方案（待评估，尚未动手）：

1. **用 `HoughCircles` 检测中圈**替代 `fitEllipse` 轮廓拟合
   当前失败根因是中圈白线被球员 / 草皮条纹打成**断续弧线**，`findContours` 拿不到完整闭合轮廓。`HoughCircles` 对不完整圆更鲁棒。
2. **放宽椭圆检测的 `center_dist` 惩罚**
   镜头 pan 后中圈本就该偏离画面中心，当前 `score = ar * (1.0 - center_dist * 0.5)` 是反效果。
3. **改用特征点跟踪**
   用光流持续跟踪首帧标定的中圈区域，逐帧得到新位置，不依赖重新检测。
4. **降低或关闭重标定频率**
   若镜头其实基本不动，每 10 帧重标定只是徒增噪声与耗时。

### Step 1.5：确认「越位线偏」是否源于点击位置

已确认**点击顺序**不影响结果（代码按 `p1_py > p2_py` 自动判断远近，见 4.7）。
但如果两点**位置**本身没点在白线上，H 会直接偏。可核对 `output/debug_log.txt` 里记录的 H 与 world 坐标做验证。

### Step 2：给 VP 路径的三个静默 return 加日志

当前 `return` 不打印任何东西，无法区分是"直线检测失败"、"VP 质量低"还是"`_build_from_vp` 返回 None"。这是排查效率的主要障碍。

位置：`video_processor.py` 的 `_recalibrate_from_vp`（约 651-680 行）。

### Step 3：验证新重标定路径是否真的生效

跑 150 帧，逐项确认：

1. 日志里出现 `[Recalib Ellipse Frame X] ACCEPT H (angle=..)` —— 重标定真正跑通
   （对照：本次 14 次全是 `Ellipse not detected`，一次 ACCEPT 都没有）
2. 镜头摇动后越位线**能跟着走** —— 对比摇动前后若干帧的 H，应当随之变化
3. 越位线**位置**仍正确 —— ⚠️ 3° 门控只查球门线夹角，**查不出平移**，必须人工看图确认，不能只看日志

### Step 4：修旧点击点 bug（4.4）

仅在 VP 路径能成功之后才需要处理。修法：
1. VP 路径改用当前 H 投影出的中圈位置（世界坐标 (34, 52.5±9.15) 固定，投影点会跟着镜头走），而非第一帧死点
2. 重标定成功后同步更新 `_field_result.keypoints`

### Step 5：排查越位判定异常（4.6）

150 帧 77 帧越位 + 防守方只识别 1-3 人，独立隐患。

---

## 八、环境备忘

- **PowerShell 中文路径有编码问题**，`cd "d:/workBuddy/越位识别"` 会报路径不存在（乱码成 `瓒婅綅璇嗗埆`）
- 但 shell 的**默认 cwd 就是项目根目录**，用相对路径即可，git / python 都能直接跑
- `test_run.py` 存在的意义就是用 Python 内部拼路径传给子进程，绕开 shell 解析

### test_run.py 用法（本次已改造）

```bash
python test_run.py                                  # 13 号视频，auto 标定，全片
python test_run.py 12                               # 指定视频编号 11/12/13/14
python test_run.py 13 --calib-mode center_circle    # center_circle（自动找标定文件）
python test_run.py 13 --max-frames 150              # 只处理前 150 帧
python test_run.py 13 --calib-mode auto --max-frames 150
```

改造点：
- `--calib-mode` / `--calibration` 等带值参数成对透传，不再与默认 `--calib-mode auto` 重复冲突
- `--calib-mode center_circle` 时自动查找 `output/<视频名>_center_circle_calib.json`，省去每次手动点击
- 其余参数（--resize / --every / --skip / --confidence 等）原样透传

### 常用诊断命令

```bash
# 只看法标定相关日志
python test_run.py 13 --calib-mode center_circle --max-frames 150 2>&1 | Select-String -Pattern "Recalib|Ellipse|Calib|Error|Traceback"

# 语法检查
python -m py_compile offside_detector/*.py
```

### 关键日志含义速查

| 日志 | 含义 |
|------|------|
| `Ellipse not detected` | 椭圆路径失败 → 会回退 VP 路径 |
| `[Recalib Ellipse Frame X] REJECT H (angle=..)` | 椭圆找到了但被 3° 门控拒 |
| `[Recalib Ellipse Frame X] ACCEPT H (angle=..)` | 椭圆路径成功 |
| `[Recalib VP Frame X] VP=(..) quality=..` | VP 路径成功 ← **本次一次都没出现** |
| `[Recalib Auto Frame X] N/32 kpts RMSE=..` | auto 模式重标定成功 |
| `[Recalib Auto Frame X] REJECT H` | auto 检测到了但被门控拒 |

### 关键文件位置

| 内容 | 位置 |
|------|------|
| 重标定入口 + 三条路径分发 | `video_processor.py:482-502` |
| 椭圆重标定 | `video_processor.py:504-552` |
| auto 重标定 | `video_processor.py:554-641` |
| VP 重标定（含旧点击点 bug） | `video_processor.py:643-709` |
| 质量门控辅助函数 | `video_processor.py:45-91` |
| 中圈椭圆检测 | `ellipse_calibrator.py:44-140` |
| VP 约束建 H（接收旧点） | `field_detector.py:693-748` |
| 球场线检测 | `pitch_keypoint_detector.py:701-741` |

### 诊断图（上次遗留，本次已看过）

| 图 | 结论 |
|------|------|
| `output/calib_white_lines_mask.png` | 中圈白线能提取，但是断续弧线 |
| `output/calib_ellipse_detect.png` | 原始帧，中圈在画面左侧偏上 |
| `output/calib_lines_detect.png` | 直线检测成功，但线几乎平行（无消失点） |

---

*进度备忘结束。下次开工：读第七节 Step 1（新）—— 给 center_circle 换一条能用的重标定路径。
注意 auto 模式已被确认不可用，不要再去试。*
